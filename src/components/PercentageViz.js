import React from "react";

const PercentageViz = ({ submarkets, values }) => {
  let { _id, _full_text, county, geoid, name, ...percentages } = values;

  return (
    <div>
      <div>test</div>
    </div>
  );
};

export default PercentageViz;
